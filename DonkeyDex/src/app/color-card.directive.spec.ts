import { ColorCardDirective } from './color-card.directive';

describe('ColorCardDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorCardDirective();
    expect(directive).toBeTruthy();
  });
});
